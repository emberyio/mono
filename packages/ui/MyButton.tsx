import { Button } from "@chakra-ui/react";
import * as React from "react";

// tên comopnent này k dc trùng với component trong chakra-ui
export const MyButton = () => {
  return (
    <Button variant="outline" colorScheme="linkedin">
      Boop
    </Button>
  );
};
